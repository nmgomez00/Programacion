import { useState } from "react";

export function Contador() {
  const [count, setCount] = useState(Array().fill(""));

  return (
    <>
      <input type="text" name="Tareas" va />
    </>
  );
}

export default Contador;
